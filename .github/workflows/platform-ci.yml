name: platform-ci

on:
  pull_request:
    paths:
      - 'services/platform-service/**'
      - 'shared/**'
      - 'Directory.*.props'
      - 'global.json'
  push:
    branches: [main, tier-5-microservices]
    paths:
      - 'services/platform-service/**'
      - 'shared/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with: { dotnet-version: '10.0.x' }
      - run: dotnet restore services/platform-service/Platform.sln
      - run: dotnet build services/platform-service/Platform.sln --no-restore -c Release
      - run: dotnet test services/platform-service/Platform.sln --no-build -c Release --logger trx
